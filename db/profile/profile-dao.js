const model = require('profile-model');


const updateProfile = (email, profile) => {};
const createProfile = (profile) => {};

// for Admin?
const findAllProfiles = () => {};
const deleteProfile = (email) => {};


module.exports = {
    updateProfile, createProfile,
    findAllProfiles,deleteProfile
};

